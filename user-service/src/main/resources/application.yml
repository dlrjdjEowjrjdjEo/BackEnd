server:
  port: 8762

spring:
  application:
    name: user-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/todaypills?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: root #수정 요망
  jpa:
    hibernate:
      ddl-auto: update