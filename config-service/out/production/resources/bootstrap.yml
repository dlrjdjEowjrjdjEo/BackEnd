server:
  port: 8888

spring:
  main:
    web-application-type: reactive
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: git@github.com:medicMSA/Configuration.git
          default-label: main #브랜치명
#          ignore-local-ssh-settings: true
          strictHostKeyChecking: false
#          private-key: |
management:
  endpoints:
    web:
      exposure:
        include: health, busrefresh